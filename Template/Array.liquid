import { {% for meta in metas %} {{ meta.Name }}, {% endfor %} } from './../common/commonCommand.js'

export default {
{% for meta in metas %}
{% if meta.Name != null %}
    {{ meta.Name }}(){
        return {{ meta.Name }}wrapper({
            operation: {{ meta.Desc }}
        })
    }
{% endif %}
{% endfor %}
}


